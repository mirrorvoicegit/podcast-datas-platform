# 更新紀錄（CHANGELOG）

各工具版本**分開記錄**（v13.1 起的制度）。每次更新時，維護的 AI 在對應工具的段落最上方加一條。版本號自 v14 起**改回顯示在入口頁**（各工具卡片內，樣式與其他資料列一致、不特別強調），這份檔案仍是完整紀錄處。

---

## 平台共用（index.html）

### 2026.07.20
- 入口頁標題「節目組數據工具」改為「節目組工具箱」
- 第二排新增「Podcast 報獎雷達」卡片，連到 `https://mirrorvoicegit.github.io/podcast-awards/`（正式版，另一個獨立 repo）；第一排三張既有卡片順序不變
- 版本號待補：對方 repo 版本號需維護者提供後回填卡片內「版本」欄位

### v14.3 — 2026.07.08
- 入口頁標題文字「鏡好聽節目組專用」改為「鏡好聽專用」（同步修改 tool-1／tool-2 頁面的返回連結字樣、tool-1 匯出報表標題列）

### v14.2 — 2026.07.08
- 入口頁改版（方向 B「流動玻璃」）：柔和動態漸層背景＋毛玻璃卡片，取代原本的紙感編輯室風格；範圍**僅限入口頁**，tool-1／tool-2／匯出報表維持原有風格不變
- 三張工具卡片改為同排（3 欄），逐列（標題／各欄位／按鈕）精確對齊：欄位值改用固定寬度標籤欄＋靠左對齊（原本是 `justify-content:space-between` 只有右邊對齊，左邊參差不齊）；CTA 按鈕改固定高度，不再受按鈕內文字/圖示字級不同影響
- 全站標題重新命名：入口頁「節目組數據工具」；「RSS節目收聽數據分析」→「RSS節目收聽分析」；「鏡好聽平台收聽數據分析」→「鏡好聽平台收聽分析」（index、tool-1、tool-2、使用說明、匯出報表標題、彙整表上傳識別字串同步更新）

### v14.1 — 2026.07.08
- 「節目數據總覽」（tool-3 佔位）卡片改回跟其他卡片一樣的正方形尺寸，不再橫跨整行
- 清除從未使用的 `.masthead .subtitle`、`.tool-label` 死碼 CSS

### v14 — 2026.07.08
- 全站新增 favicon（耳機圖示，站內色系：米白底、墨黑線條、Apple 紅點綴）
- 版本號改回顯示在入口頁各工具卡片內（與資料來源等欄位同樣式，不再獨立醒目呈現）
- 新增「節目數據總覽」卡片（tool-3 佔位，標示「開發中」，非連結）

---

## Tool-1：RSS節目收聽分析

### v16 — 2026.07.20
- **新增 YouTube 影音版雙來源支援**：除既有 Apple／Spotify／YouTube(Podcast 版) 外，可額外上傳第 4 個來源「YouTube 影音版」，兩份 YouTube 資料會分開儲存（`youtubePodcast`／`youtubeVideo` 兩個獨立欄位，不預先合併成單一 YouTube 總數）
- 支援直接拖曳 YouTube Studio 匯出的 ZIP（只解析其中的 `表格資料.csv`，不讀 `圖表資料.csv`／`總計.csv`），也可上傳解壓後的 CSV；新增唯一相依套件 JSZip（CDN 固定版本 `3.10.1`，純前端瀏覽器內解壓，不上傳檔案、不加後端）
- 新增 EP 集數編號辨識（`extractEpisodeNumber`，與標題正規化共用同一套規則），支援 EP108／EP 108／Ep108／ep.108／EP-108／第108集／第 108 集等寫法；EP 編號是配對的最強訊號但不是唯一條件，仍受同節目範圍與標題相似度約束
- 兩份 YouTube 檔案存在時，一律顯示「角色確認」畫面（新增步驟一之二）：依檔名關鍵字（如「影音版」「video」）與集數規模給出建議角色，但**只是建議、不會自動套用**，一定要使用者手動選擇或互換後才能繼續；未選完或兩邊選成同角色時無法繼續
- YouTube 兩版本跨版本配對：先用 EP 編號精準配對（同檔內重複 EP 不自動選）；配不到的再併入既有的「可疑配對」機制（原本只處理跨平台孤兒，本次泛化成同時處理 4 種來源），一樣要人工確認
- 修正 YouTube「觀看次數」空白時被 `parseInt(...) || 0` 靜默吃成 0 的 bug：現在空白會保留為缺資料（顯示 —），不會誤判成真的 0；本次用裴社長 Podcast 版 EP108（實際存在空白觀看次數）驗證修復生效
- 有影音版資料時，報表新增第 4 欄位／第 4 種圖表資料集（六位數色碼 `#9a9a9a`）；沒有影音版的節目，畫面與匯出報表維持原本三來源版面，不多顯示空欄
- 主程式與「匯出獨立 HTML」內嵌副本（`exportStandaloneHTML`）同步支援上述所有變更
- 順便修正：TOP10 排行榜等長條圖不論項目數量多寡，長條粗細改為固定（`maxBarThickness:26`），避免項目少時長條被拉得肥大
- 上傳說明文字微調：標題改「上傳從平台匯出的 CSV 或 ZIP」；拖曳區提示補充「若有影音版可上傳第 4 個檔案，檔名含『影音』『影音版』『video』等字眼會被優先參考」
- 測試：`node -c`、div 配對、重複 id 檢查全過；`dev/verify.js` 既有 17 項核心檢查全過（無影音版路徑零回歸，僅表頭欄數由 8→9，因新增欄位固定存在、僅用 CSS 依 `has-video-source` 顯示/隱藏）；另用維護者提供的兩份裴社長《吃喝玩樂》真實 YouTube Studio ZIP（Podcast 版 108 集、影音版 48 集）做端對端驗收：JS 全程無例外、48 集影音版全數配對成功、EP108（Podcast 版 `fP2-KGeFm0E` × 影音版 `D3fBntQLLtw`）正確配成同一集、影音版觀看次數 64 正確帶出、Podcast 版該集空白觀看次數正確顯示為 —（非 0）
- 尚存限制：`dev/make-fixtures.js` 尚未新增「兩份 YouTube 假資料」產生器，目前雙來源情境只用真實裴社長 ZIP 手動跑過一次，沒有納入 `dev/verify.js` 的自動化回歸；未來若要再動這塊邏輯，建議先補上假資料產生器與對應自動檢查

### v15 — 2026.07.09
- **整段移除「上傳摘要到彙整表」（v13 起）與「AI 觀點」（v14 起）兩個功能**，含程式碼、UI、說明文字
- 決策理由（維護者拍板）：彙整表整體邏輯（怎麼跟未來 tool-3 分工、資料怎麼累積）還在重新思考中，先把舊方案連同依賴它的 AI 觀點一起拿掉，避免留著半套功能，等想清楚了再重新設計、重做更乾淨
- 移除範圍：
  - `tool-1/app.js`：`uploadToSheet`／`state.uploadSnapshot`／`SHEET_CFG_KEYS`／`DEFAULT_SHEET_TOKEN`／`DEFAULT_SHEET_URL`／`generateAiInsight`／`buildAiPrompt`／`renderAiOutput`／`AI_CFG_KEYS` 等全部相關函式與呼叫點
  - `tool-1/index.html`：彙整表設定面板（`.sheet-upload-box`/`.sheet-settings`）、AI 觀點區塊（`#ai-insight-block`）、對應 CSS，以及頁面/頁尾提到「唯一例外」「上傳摘要到彙整表」的說明文字
  - `apps-script/彙整表.gs` 整個檔案與 `apps-script/` 資料夾
  - `dev/verify-ai.js`（整支只測 AI 觀點功能，功能移除後無測試對象，一併刪除；`dev/README.md` 同步移除相關段落）
  - 入口頁 `index.html` 使用提醒的「唯一例外」條目
- 程式碼仍完整留在 git 歷史，未來重新設計彙整表機制時可回頭取出參考，不必從零想
- 驗證：`node -c`、div 配對、重複 id 檢查全過；`dev/verify.js` 17 項核心檢查全過（移除過程中抓到一處殘留呼叫 `setSheetStatus`，已一併清除）

### v14.5 — 2026.07.08
- **撤回 v14.4「流動玻璃」改版**，背景與面板改回原本的紙感編輯室風格（純色背景＋極淡紙紋、不透明卡片）
- 決策理由（維護者拍板）：tool-1 是逐列比對數字的工作介面，跟一次性瀏覽的入口頁性質不同——持續飄移的背景動畫與半透明面板會拉走閱讀時的周邊視覺注意力、降低長時間讀表時的視覺穩定性，優先順序是「好讀」高於「好看」
- 做法：`tool-1/index.html` 直接還原到 v14.4 之前的版本（`git checkout` 舊 commit），非手動重寫，逐字元與改版前一致
- 玻璃改版程式碼仍完整留在 git 歷史（v14.4 commit），未來若想重新評估可直接取出，不必重做
- 入口頁卡片版本號同步顯示 v14.5

### v14.4 — 2026.07.08
- 視覺改版，比照入口頁「流動玻璃」方向（方向 B）：柔和動態漸層背景＋毛玻璃感面板，取代原本紙感編輯室風格
- 範圍：上傳區、控制列、期間區塊、節目觀察／AI 觀點區塊、圖表區塊、彙整表設定、橫排數字、訂閱數列、數據摘要、按鈕全部改玻璃感／圓角／膠囊造型
- **刻意不動**：單集總表本體（`.table-wrap`／`#fuzzy-table`／`#orphan-table`）與 tooltip 相關樣式，避免動到 CLAUDE.md 記錄過的 tooltip 舊病；匯出版因共用同一份 `<style>`，會自動套用同樣風格，`.standalone-header` 未受影響
- 正式套用前已在獨立沙盒試跑全套自動化流程（上傳／產出報表／匯出／排序／搜尋／圖表初始化），17 項檢查全過，畫面另有截圖確認才套進正式檔
- 入口頁卡片版本號同步顯示 v14.4

### v14.3 — 2026.07.08
- 隨全站文字修改：返回入口連結、匯出報表標題列「鏡好聽節目組專用」改為「鏡好聽專用」

### v14.2 — 2026.07.08
- 隨全站改名：頁面標題、`<h1>`、匯出報表標題、彙整表上傳識別字串（`tool` 欄位）統一改為「RSS節目收聽分析」

### v14.1 — 2026.07.08
- 修版本號沒同步更新的 bug（卡片內誤留 v13.2 字樣）
- 上傳三個 CSV 的說明文字大幅精簡（原本四句擠成一段，且「自動」一詞連續出現三次）
- 拖曳區新增明確的「已上傳完成」狀態：框線變實線綠色＋「✓ 已上傳 N 個檔案」提示，移除全部檔案後自動恢復原狀
- 清除多處確認為死碼的 CSS（可疑配對表有兩份重複定義、期間改下拉選單後留下的舊按鈕樣式、從未接上 HTML 的訂閱數說明收合框、空狀態插圖樣式等），tool-2 也一併清了兩處（`.info-box`、`.platform-mini`）——純樣式清理，未動任何計算邏輯

### v14 — 2026.07.08
- 新增「AI 觀點」（測試功能）：報表產出後可手動觸發，把摘要數字與本期單集標題送到使用者自己的
  Google Gemini API 金鑰進行分析，回傳內容標示「僅供參考、非正式結論」；金鑰存於使用者瀏覽器
  localStorage，不上傳。這是本工具除彙整表外第二個會把資料送出瀏覽器的功能，需使用者主動觸發。
- AI 觀點若已產生，隨「下載報表」凍結進匯出檔（比照節目觀察的凍結方式）；未產生則匯出檔不留空殼。

### v13.2 — 2026.07.08
- 單集總表「本週」標籤改為「最新一週」，移到日期正下方置中
- 版本紀錄自入口頁移除（只留 CHANGELOG.md），全站加 noindex meta 與 robots.txt 謝絕搜尋引擎收錄

### v13.1 — 2026.07.08
- 「收聽平均比較」欄改為整欄靠左對齊，上下箭頭對齊
- 「時間趨勢線」右上圖例改為實心色塊，與「開播至今播放排行榜 TOP 10」一致
- 彙整表上傳改為按「下載報表」時自動觸發（移除獨立上傳按鈕）；通行碼內建，製作人只需填一次網址
- 彙整表改以「節目＋日期」為單位：同一天重複下載直接覆蓋當天紀錄，不同天新增一列

### v13 — 2026.07.08
- 新增「上傳摘要到彙整表」：報表摘要數字（不含逐集明細、備註、原始檔）送到節目組自己的 Google 試算表
- 新增 `apps-script/彙整表.gs`：接收上傳＋主管總覽頁
- 「資料不離開電腦」承諾文字明示唯一例外

### v12 — 2026.07.08
- 修時間趨勢線 YouTube 圖例黑塊（三位數色碼＋透明度後綴＝無效色）
- 新增「開播至今播放排行榜 TOP 10」圖表（不受分析區間影響）
- 單集總表新增「收聽平均比較」欄（紅↑高於開播至今單集平均、綠↓低於、缺平台顯示 —）
- 橫排數字新增「開播至今單集平均」
- 平均基準會重套使用者確認過的可疑配對
- 新增 `dev/` 自動驗證配備（無頭瀏覽器端對端測試）

### v11.3 — 2026.06.29
- 刪「前後半段平均」假結論；修錯字；摘要區雙橫線

### v11～v11.2 — 2026.06
- 製表人欄位、單集備註欄、多關鍵字搜尋、修檔名日期時區
- 上傳區改卡片清單風格、修拖曳區塌陷、文字全面統一、入口頁大改

### v10 — 2026.06
- 修「非數值」bug；多檔拖曳自動辨識平台；刪「月初至今」期間

### v9～v9.3 — 2026.05~06
- 期間下拉選單（依上架日）；節目名稱／訂閱數記憶；tooltip；修 toISOString 時區少一天；刪 Apple 排名捏造內容

### v8 — 2026.05
- 兩工具整合進統一平台

---

## Tool-2：鏡好聽平台收聽分析

### v11.5 — 2026.07.08
- 隨全站文字修改：返回入口連結「鏡好聽節目組專用」改為「鏡好聽專用」
- 順手修 v11.4 改名時漏改的殘留：匯出報表標題列仍寫死舊字樣「鏡好聽節目組專用」「鏡好聽平台收聽數據分析報告」，一併改為「鏡好聽專用」「鏡好聽平台收聽分析報告」（純文字，計算邏輯未動）

### v11.4 — 2026.07.08
- 隨全站改名：頁面標題、`<h1>` 改為「鏡好聽平台收聽分析」（純文字，功能未動）

### v11.3 — 2026.06.29
- 改名「鏡好聽平台收聽數據分析」
- 新增製表人欄位（獨立記憶 key）
- 收聽區間右上對齊式呈現、搜尋多關鍵字、匯出 header 一致

### v9 — 2026.05
- 數據說明 tooltip

### v8 — 2026.05
- 兩工具整合進統一平台（訪客獨立統計、追蹤清單、排行榜、多檔上傳、跨期趨勢為 v8 前開發期完成）
